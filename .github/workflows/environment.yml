name: Environment vatiables
on: push
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      BRANCH: main
      USERNAME: ${{ vars.USERNAME}}
    steps:
      - run: echo "${USERNAME}"
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
